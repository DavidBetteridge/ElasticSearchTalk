All
GET	/_search

Single Index
GET /meetups/_search

Match Search
POST /meetups/_search
{
    "query" : {
        "match" : { "speakers": "Benjamin"} 
    }
}

Term Search (no results)
POST /meetups/_search
{
    "query" : {
        "term" : { "title": "Double Bill - Elasticsearch and JavaScript Performance"} 
    }
}

Term Search (one result)
POST /meetups/_search
{ 
	"query" : {
        "term" : { "_id": "june2018"} 
    }
}

View Mapping (keyword)
GET /meetups/_mapping

Term Search (fixed)
POST /meetups/_search
{
    "query" : {
        "term" : { "title.keyword": "Double Bill - Elasticsearch and JavaScript Performance"} 
    }
}

Stackover Flow
POST /stackoverflow/_search
{
  "query": {
    "match": {
      "Body": "Unicorn"
    }
  }
}